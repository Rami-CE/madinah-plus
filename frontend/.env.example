# Optional: leave blank for Vite proxy to localhost:5088
# Production Workers build uses Railway API (see root package.json build:cf)
# VITE_API_URL=https://api-production-8d38.up.railway.app
VITE_API_URL=
